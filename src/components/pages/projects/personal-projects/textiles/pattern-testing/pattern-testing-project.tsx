import ProjectPage, { ProjectPageProps } from "../../../project-page";

export default function PatternTestingProject(props: ProjectPageProps): JSX.Element {
    return <ProjectPage {...props}>
    </ProjectPage>
}
