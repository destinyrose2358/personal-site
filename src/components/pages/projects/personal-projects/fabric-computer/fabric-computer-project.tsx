import ProjectPage, { ProjectPageProps } from "../../project-page";


export default function FabricComputerProject(props: ProjectPageProps): JSX.Element {
    return <ProjectPage {...props} >
    </ProjectPage>
}
