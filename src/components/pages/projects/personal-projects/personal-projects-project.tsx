import ProjectPage, { ProjectPageProps } from "../project-page";

export default function PersonalProjectsProject(props: ProjectPageProps): JSX.Element {
    return <ProjectPage {...props} >
    </ProjectPage>
}
