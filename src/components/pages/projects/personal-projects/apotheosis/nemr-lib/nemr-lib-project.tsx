import ProjectPage, { ProjectPageProps } from "../../../project-page";

export default function NEMRLibProject(props: ProjectPageProps): JSX.Element {
    return <ProjectPage {...props} ></ProjectPage>
}
