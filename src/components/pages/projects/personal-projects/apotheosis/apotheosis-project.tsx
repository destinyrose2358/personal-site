import ProjectPage, { ProjectPageProps } from "../../project-page";


export default function ApotheosisProject(props: ProjectPageProps): JSX.Element {
    return <ProjectPage {...props}>
    </ProjectPage>
}
