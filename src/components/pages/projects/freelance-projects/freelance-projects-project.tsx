import ProjectPage, { ProjectPageProps } from "../project-page";

export default function FreelanceProjectsProject(props: ProjectPageProps): JSX.Element {
    return <ProjectPage {...props} >
    </ProjectPage>
}
